class Rx : public RcPeer {  
public:
  Rx(MacAddress mac_tx, uint8_t channel, wifi_interface_t iface, const uint8_t *lmk, Logger *log) : RcPeer(mac_tx, channel, iface, lmk) { 
    failsafe();
	logger = log;
  }
void update() {
      ....
      if ((now - last) > FAILSAFE_TIME) {
	    failsafe();
		.....
private:
  void failsafe() {
    throttle.failsafe(); chan1.failsafe(); chan2.failsafe(); chan3.failsafe(); chan4.failsafe(); 
  }
   
combat:
- extra receiver
- use virtual mac-address
  of one Tx and N Rxs: only Tx needs mac table
- offset motor with o-rings
- round sharp corners
- extra holes or in motor mount
- use wind channel to cool esc using tapered pvc tube
  channel in flight direction/ribs
- receiver usb should be accessible for updates

hall sensor or pot and output a 0.8–4V throttle signal
use simple crank and slider, max 180 degress and not linear
test 90 degrees rotation of 300 potmeter
Rack and pinion stl?

carrier
- new bigger bellcrank, move backwards to 2nd reenforcement
  reuse elevator boom
  hoogteroer: scharnieren en normaal horntje  

- Vbat low blijven piepen en is te zacht voor in broek

- generic logging (like blackbox) on C6
- Nex Tx charge by usb with sealing cap.
  Is this possible, needs 'step' in wall for access usb connector?
- Tx led en laad connector (balance?)
- Lost count tx klopt niet is altijd zero
- No tx -> rx log status lipo etc

- todo test A4 (MTMS) to C2 and A6 (MTCK) to C3
- Small tx pcb: no connectors, no diode
  size = size C6 + one collumn left and right
  Rs below C6, in middle??
  4 corners of C6 extra black spacers if more clearance needed
  use JST PH connector for VBat or solder connection for JST-PH red connector
  use possibilty for horizontal Esc servo connector
  Ideas: 
  - use on both side of esp JST 2.0 SH connectors max 7 pins, is it possible to add connector with 1mm space in Frizting 
  - special Rx pcb, one side with 2 horizontal servo connector (Esc and chan1)
- idea? No PCB version for simple (TH + THR + hook) Tx handle?? Only two Rs needed.
  every connection its own wire? 
  using flat cable with connector?
  small form factor add-onn box for handle (ESP + 2 switches + potmeter) 
  same idea for simple Rx?? with flat cable 
  flat flexible cable (ribon cable) pitch JST SH 1.0? mm: 
  - minimal 11: 5v, 3.3V, GND, TxBatt+, TxBatt-, Pot/Esc(A0), Chan1(A1), VBatt+(A2), TH(D6), SW1(D5), Beep(D7)
    extra GNDs?
  - extra or solder direct to C6: SW2(D4), Chan2(A4), Chan3(A5), 
  JST 2.0 SH 12 pins is too big??, split in multiple cables?
  https://www.amazon.com/Connector-Housing-Connectors-Controller-Pre-Crimped/dp/B0DH29DLMT?th=1 (only female?)
  https://www.amazon.com/JST-PH-Connector-Silicone-Cables-Female/dp/B0DMZK9CJR/ref=sr_1_8?crid=MT6HGLPC9VJZ&dib=eyJ2IjoiMSJ9.22ctwhKmek3DiFS-5XVMsXGbeI3-omfk9mRUkfeaNABnQzmW5oKmaoz1XlHvkzOo9htcidf4y4TUtq-Xo5kkeZN5iZUDuKYFrj_vpwloWuDyusCmvZY51jdJFkIlWnuy47jNHrdYRGmsAk-6dXCD0M75bkS1rrevnCZzEH4P-ZVHdQcjvH3PDpPYcAJ_-eC62aZUmcTehIAHSnyzcsrGD2slWpDJU7y0r0of80syBBw.4gBcA8al8x5gMmjoPLTOSubBzcVO2WzvjBcOZoam4Bo&dib_tag=se&keywords=jst%2Bsh%2Bcable%2Bwith%2Bmale%2Band%2Bfemale%2Bconnector%2B12%2Bpins&nsdOptOutParam=true&qid=1779263925&sprefix=jst%2Bsh%2Bcable%2Bwith%2Bmale%2Band%2Bfemel%2Bconnector%2B12%2Bpins%2Caps%2C194&sr=8-8&th=1
  https://www.amazon.com/JST-PH-Connector-Silicone-Cables-Female/dp/B0DMW7FPCH/ref=sr_1_8?crid=MT6HGLPC9VJZ&dib=eyJ2IjoiMSJ9.22ctwhKmek3DiFS-5XVMsXGbeI3-omfk9mRUkfeaNABnQzmW5oKmaoz1XlHvkzOo9htcidf4y4TUtq-Xo5kkeZN5iZUDuKYFrj_vpwloWuDyusCmvZY51jdJFkIlWnuy47jNHrdYRGmsAk-6dXCD0M75bkS1rrevnCZzEH4P-ZVHdQcjvH3PDpPYcAJ_-eC62aZUmcTehIAHSnyzcsrGD2slWpDJU7y0r0of80syBBw.4gBcA8al8x5gMmjoPLTOSubBzcVO2WzvjBcOZoam4Bo&dib_tag=se&keywords=jst%2Bsh%2Bcable%2Bwith%2Bmale%2Band%2Bfemale%2Bconnector%2B12%2Bpins&nsdOptOutParam=true&qid=1779263925&sprefix=jst%2Bsh%2Bcable%2Bwith%2Bmale%2Band%2Bfemel%2Bconnector%2B12%2Bpins%2Caps%2C194&sr=8-8&th=1- 100Hz?
- beep 1x if killswitch deactivated
- if not connected log every N secs status for Tx and Rx
- add cmd to toggle on/off login, needed when setting params
- Tx starts beeping after x mins not used
- not needed, is done with hardware jumper: add config param for Tx chan4 servo or digital input
- test with C6 devkit debugging on new breadboard, use wire or dipswitch for TH
- test new software on prototype boards
  Rx: A0 thr, A2 vbat
  Tx: A0 esc, A1 vbat, D3 TH, D8 beep
- if Rx is exchanged only change in to macpeer in Tx is needed?
  use special header to populate list (index??) name-model/tx macaddress
  reboot afterwards
- make list with peers to select from to bind??
- ??? Tx: if no need for charge, can use 5V C6 pin for power??
- make better photos of protoboards and screenshot of BL logging
- ?? softstart option for CLtimer and combat handle (= startup ramp %)
  rampup time if starting from zero
   most ESCs have a similar function
- exponential throttle curve
  can use (limited) floats?? lookup table?? N knee points with linear interpolation
- idea extra switch(es) on Tx box with battery on belt and fixed cable to handle
- mini reset button for binding or terminal app?
https://opencircuit.nl/product/mini-schakelaar-pcb-mount-2-pin-spst-50ma?srsltid=AfmBOophvi_jH0liToo5tpH97ShfAzFRpg4mTHkbKVPoL-LJvQN6kpsW
https://opencircuit.nl/product/tactiele-button-3x6x5mm

ON OFF ON SPDT switch wiring
                 Throw 1 ----- GND
                 Throw 2 ----- 3.3V
                 Common  ----- ADC pin
3.3V ----[ Rtop ]----+----[ Rbottom ]---- GND
                      |
                    ADC pin
	Common → ADC pin
	One end → GND
	Other end → 3.3 V
	Middle position = OFF → no connection → ADC only sees the resistor divider.
Resulting voltages
	Position 1 (to GND): ADC ≈ 0" V" (switch dominates, low impedance to GND)
	Middle (OFF): ADC ≈ 1.65" V" (pure divider)
	Position 3 (to 3.3 V): ADC ≈ 3.3" V" (switch dominates, low impedance to VCC)
Now you truly get three stable, well separated levels.
Use 20K Rs!

- sometimes warning: adc_oneshot_get_calibrated_result(328): adc oneshot read fail
- move repro to https://codeberg.org/??
- esp-now analyser https://wiki.wireshark.org/ESP_Preferences

CLTimer: 
- Update to latest version of OpenRC4CL_util.h
  Beeper warning signal N secs before starting engine
  Abort starting sequence and running with button
- softstart option for CLtimer and combat handle (= startup ramp %)
  rampup time if starting from zero

fase2:
- exponential throttle
- use state machine to keep track of status and actions
- for documentation: differentiate between to thr warning systems
  note warning are NOT synchronised (yet), a protocol is needed
  - Tx: beep warning after maxTime expired
  - Rx: esc warning before maxTime expires and kill engine at maxTime
- note: chan 2/3/4 can be used for on/switch
- pinExternLed D8/9/10? or use chan3?? or only led indicating bat connected?
- add R1 and R2 to NVS, can be used for tuning V calib usig ohm meter
- this needed is only needed if rearranging order is needed
  Rename class Switch to SwitchDI and make SwitchAI
  Note dangling middle=dangling 0V, L/R= 1.65V/3.3V, use Tx software to put in 'ascending order'
- alternative Rx: create NVS param and use map(pin table)
  rearrange using: int* map = &rc.chan1  (is ptr to int[4])
- 2 or 3 position switches on Tx are also possible using reading the normal channels and analoge input
  using two Rs for voltage divisor
- add channel mapping on Rx default "[0,1,2,3,4]"
  map channel order right after packet is received
- pair using wifi breadcast mode pair ble_name -> mac_addr
- Tx for all chans add 
  - offset middle position
  - min and max value
- new Failsafe throttle implementation: always for 2(?) sec toggle 0.5 sec to half power and than TH, no need for timer only wall clock
- soft mac address for Rx (and Tx??)
- (paper display on TX)

(fase 3:
- use IMU as crash sensor to kill engine
- governor for throttle
- see for ideas https://www.keithrenecle.co.za/Electric%20CL.htm#Govtimerinfo 
- https://howtomechatronics.com/how-it-works/how-brushless-motor-and-esc-work/
- https://www.digikey.com/en/articles/controlling-sensorless-bldc-motors-via-back-emf
- https://www.rcgroups.com/forums/showthread.php?1006124-Brushless-rpm-Sensor
- hall sensor??
- idea measure freq zero crossing
- pcb https://fritzing.org/learning/tutorials/designing-pcb/
  https://community.aisler.net/t/our-simple-pricing/102
)

Changed:






Idea for mac table (if bind with broadcast is not possible)

class MAC_tab {
public:
  MAC_tab() {};  read from nms  (own space with name mac)
  // not needed??  int nrElms() { return nr; }
  int add(char* name, char* mac) {
    // check max elems
    tab[nr].name = strcpy(name); ?? check length smaller than max
    tab[nr].mac = strcpy(mac); ??  check length exactly
	add to nvs 
    return nr++;
  }
  char* mac(char* name) {
    find name in tab and return mac
  }
private:
  struct MAC_ELM{ char name[11]; char mac[17]};
  int nr = 0;
  MAC_ELM tab[16];
}


Define a POD style class (or struct)
To store an object as a blob, it must be trivially copyable (no pointers, no virtual methods, no dynamic memory).

struct DeviceConfig {
    uint32_t version;
    int32_t threshold;
    float gain;
    bool enabled;
};

#include <Preferences.h>

Preferences prefs;

DeviceConfig config = {
    .version = 1,
    .threshold = 42,
    .gain = 0.75f,
    .enabled = true
};

void saveConfig(const DeviceConfig &cfg) {
    prefs.begin("app", false);  // RW mode

    prefs.putBytes("config", &cfg, sizeof(DeviceConfig));

    prefs.end();
}

bool loadConfig(DeviceConfig &cfg) {
    prefs.begin("app", true);  // read-only

    size_t size = prefs.getBytesLength("config");
    if (size != sizeof(DeviceConfig)) {
        prefs.end();
        return false;  // no valid config stored
    }

    prefs.getBytes("config", &cfg, sizeof(DeviceConfig));

    prefs.end();
    return true;
}

void setup() {
    Serial.begin(115200);

    DeviceConfig loaded;

    if (loadConfig(loaded)) {
        Serial.println("Loaded config:");
        Serial.printf("version=%u threshold=%d gain=%.2f enabled=%d\n",
                      loaded.version, loaded.threshold,
                      loaded.gain, loaded.enabled);
    } else {
        Serial.println("No valid config found, saving defaults...");
        saveConfig(config);
    }
}

void loop() {
}